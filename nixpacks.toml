[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete - ready to start'"]

[start]
cmd = "cd python-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
