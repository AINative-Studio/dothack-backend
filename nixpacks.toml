[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["cd python-api && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "cd python-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
