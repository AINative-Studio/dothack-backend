{
  "providers": ["python"],
  "python": {
    "version": "3.12"
  },
  "install": {
    "cmds": ["pip install -r requirements.txt"]
  },
  "start": {
    "cmd": "cd python-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
